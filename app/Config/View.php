<?php

namespace Config;

class View 
{

  /**
   * Function that maps the server request to the correct controller, method, and parameters (if any). It then uses the View class to render the corresponding view in the browser window.
   * 
   * @array $request - (string $controller, string $method, mixed $param1 = null, mixed $param2, mixed $param3)
   * @return void
   */
  public function render(array $request)
  {

  }
}